const constants = require('../constants.js');

module.exports = [
    constants.DATA_ADDRESS,
    2,
    3,
    constants.formatData(500),
    constants.formatData(1000),
    constants.formatData(200),
    constants.formatData(100),
    'best',
    `# Shoot For The Moon

The only thing that matters is getting the top run. So dial up all your risk parameters to the max and get in there.

## Best run wins!`
];
